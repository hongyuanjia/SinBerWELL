SinBerFuture related energy simulations

## How to run

* Download the files in this repository
* Open `_targets.R` and run `renv::restore()` to install all packages needed
* In the R console, run `targets::tar_make()`
